version: "3"
services:
 gancio-telegram-bot:
  build: .
  image: "gancio-telegram-bot"
  container_name: "gancio-telegram-bot"
  restart: "on-failure"
  volumes:
   - ./src:/app/src
   - ./.env:/app/.env
   - ./assets:/app/assets
